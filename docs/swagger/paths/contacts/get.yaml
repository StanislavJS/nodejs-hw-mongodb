tags:
  - Contacts
summary: Get all contacts
operationId: getContacts
security:
  - bearerAuth: []
responses:
  200:
    description: List of contacts
    content:
      application/json:
        schema:
          type: object
          properties:
            data:
              type: array
              items:
                $ref: '../../components/schemas/contact.yaml'
            meta:
              $ref: '../../components/schemas/ContactsListMeta.yaml'
  401:
    $ref: '../../components/responses/BadRequest.yaml'
